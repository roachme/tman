#include <ctype.h>
#include <string.h>
#include <dirent.h>

#include "cli.h"
#include "aux/toggle.h"
#include "aux/config.h"

static int valid_desc(const char *val)
{
    if (!isalnum(*val++))
        return FALSE;
    for (; *val; ++val)
        if (!(isalnum(*val) || isspace(*val) || *val == '_' || *val == '-'))
            return FALSE;
    return isalnum(*--val) != 0;
}

static int generate_units(tman_ctx_t * ctx, char *board)
{
    struct tman_unit *units = NULL;
    char desc[100] = "autogenerated desciption for board ";

    strcat(desc, board);
    units = tman_unit_add(units, "desc", desc);
    ctx->units = units;
    return 0;
}

// TODO: add support to generate board name
static int _board_add(int argc, char **argv, tman_ctx_t * ctx)
{
    char c;
    tman_arg_t args;
    const char *errfmt = "cannot add board '%s': %s";
    int i, quiet, showhelp, status;

    status = LIBTMAN_OK;
    showhelp = quiet = FALSE;
    args.project = args.board = args.taskid = NULL;
    while ((c = getopt(argc, argv, ":hnp:q")) != -1) {
        switch (c) {
        case 'h':
            showhelp = TRUE;
            break;
        case 'n':
            //opt.board_switch = FALSE;
            break;
        case 'p':
            args.project = optarg;
            break;
        case 'q':
            quiet = TRUE;
            break;
        case ':':
            return elog(1, "option `-%c' requires an argument", optopt);
        default:
            return elog(1, "invalid option `-%c'", optopt);
        }
    }

    if (showhelp)
        return help_usage("board-add");

    if (optind == argc)
        return elog(1, "board name required");
    else if ((ctx->column = generate_column("todo")) == NULL) {
        if (quiet == FALSE)
            elog(1, "could not generate column");
        return 1;
    }

    if ((status = toggle_project_get_curr(tmancfg.base.task, &args))) {
        return status;
    }

    for (i = optind; i < argc; ++i) {
        args.board = argv[i];

        if (generate_units(ctx, args.board)) {
            if (quiet == FALSE)
                elog(1, errfmt, args.project, "unit generation failed");
            continue;
        }
        if ((status = tman_board_add(tmancfg.base.task, &args, ctx))) {
            if (quiet == FALSE)
                elog(1, errfmt, argv[i], tman_strerror(status));
            ctx->units = tman_unit_free(ctx->units);
            continue;
        }
        // TODO: add hooks
        ctx->units = tman_unit_free(ctx->units);
    }

    ctx->column = tman_unit_free(ctx->column);
    //return status == LIBTMAN_OK ? tman_pwd_board(&args) : 1;
    return 0;
}

static int _board_del(int argc, char **argv, tman_ctx_t * ctx)
{
    char c;
    tman_arg_t args;
    const char *errfmt = "cannot delete: %s";
    int i, choice, quiet, showhelp, showprompt, status;

    showprompt = TRUE;
    status = LIBTMAN_OK;
    choice = quiet = showhelp = FALSE;
    args.project = args.board = args.taskid = NULL;
    while ((c = getopt(argc, argv, ":hnq")) != -1) {
        switch (c) {
        case 'h':
            showhelp = TRUE;
            break;
        case 'n':
            showprompt = FALSE;
            break;
        case 'q':
            quiet = TRUE;
            break;
        case ':':
            return elog(1, "option `-%c' requires an argument", optopt);
        default:
            return elog(1, "invalid option `-%c'", optopt);
        }
    }

    if (showhelp)
        return help_usage("board-del");

    i = optind;
    do {
        args.board = argv[i];

        if (showprompt) {
            /* TODO: show board name.  */
            printf("Are you sure to delete board(s)? [y/N] ");
            if ((choice = getchar()) != 'y' && choice != 'Y')
                continue;
        }

        if ((status = tman_board_del(tmancfg.base.task, &args, ctx))) {
            if (quiet == FALSE)
                elog(status, errfmt, argv[i], tman_strerror(status));
        }
    } while (++i < argc);

    // TODO: update current directory if current project got deleted.
    return status == LIBTMAN_OK ? tman_pwd_board(&args) : status;
}

// TODO: show tasks in board
static int _board_list(int argc, char **argv, tman_ctx_t * ctx)
{
    tman_arg_t args;
    int c, i, quiet, status;
    const char *errfmt = "cannot list board(s) '%s': %s";

    quiet = FALSE;
    while ((c = getopt(argc, argv, ":q")) != -1) {
        switch (c) {
        case 'q':
            quiet = TRUE;
            break;
        case ':':
            return elog(1, "option `-%c' requires an argument", optopt);
        default:
            return elog(1, "invalid option `-%c'", optopt);
        }
    }

    if ((status = toggle_project_get_curr(tmancfg.base.task, &args))) {
        if (quiet == FALSE)
            elog(status, errfmt, "NOCURR", "no current project");
        return status;
    } else if ((status = tman_project_valid(tmancfg.base.task, &args))) {
        if (quiet == FALSE)
            elog(status, errfmt, args.board, tman_strerror(status));
        return status;
    }

    i = optind;
    do {
        args.board = argv[i];

        if (check_arg_board(&args, errfmt, quiet))
            continue;
        if ((status = tman_board_list(tmancfg.base.task, &args, ctx))) {
            if (quiet == FALSE)
                elog(status, errfmt, args.board, tman_strerror(status));
            continue;
        }

        for (tman_list_t * obj = ctx->list; obj != NULL; obj = obj->next) {
            LIST_BOARD_UNITS("mark", obj->name, "some board description");
        }
        ctx->list = tman_list_free(ctx->list);
    } while (++i < argc);
    return status;
}

static int _board_prev(int argc, char **argv, tman_ctx_t * ctx)
{
    return elog(1, "under development");
}

static int _board_move(int argc, char **argv, tman_ctx_t * ctx)
{
    return elog(1, "%s: under development", __FUNCTION__);
}

static int _board_set(int argc, char **argv, tman_ctx_t * ctx)
{
    tman_arg_t args;
    int atleast_one_key_set;
    int c, i, quiet, showhelp;
    const char *errfmt = "could not set board unit value '%s': %s";

    quiet = showhelp = FALSE;
    atleast_one_key_set = FALSE;
    args.project = args.board = args.taskid = NULL;
    while ((c = getopt(argc, argv, ":d:hq")) != -1) {
        switch (c) {
        case 'd':
            if (valid_desc(optarg) == FALSE) {
                elog(1, "invalid description '%s'", optarg);
                help_usage("board-set");
                return 1;
            }
            atleast_one_key_set = TRUE;
            ctx->units = tman_unit_add(ctx->units, "desc", optarg);
            break;
        case 'h':
            showhelp = TRUE;
            break;
        case 'q':
            quiet = TRUE;
            break;
        case ':':
            return elog(1, "option `-%c' requires an argument", optopt);
        default:
            return elog(1, "invalid option `-%c'", optopt);
        }
    }

    if (showhelp)
        return help_usage("board-set");
    if (atleast_one_key_set == FALSE) {
        elog(1, "gotta supply one of the options");
        help_usage("board-set");
        return 1;
    }

    i = optind;
    do {
        int status;
        args.board = argv[i];

        if ((status = tman_board_set(tmancfg.base.task, &args, ctx))) {
            if (quiet == FALSE)
                elog(status, errfmt, argv[i], tman_strerror(status));
        }
    } while (++i < argc);

    ctx->units = tman_unit_free(ctx->units);
    return 0;
}

static int _board_show(int argc, char **argv, tman_ctx_t * ctx)
{
    tman_arg_t args;
    int c, i, quiet, showhelp, status;
    struct tman_unit *units, *unitpgn;
    const char *errfmt = "cannot show project units '%s': %s";

    units = unitpgn = NULL;
    quiet = showhelp = FALSE;
    args.project = args.board = args.taskid = NULL;
    while ((c = getopt(argc, argv, ":hq")) != -1) {
        switch (c) {
        case 'h':
            showhelp = TRUE;
            break;
        case 'q':
            quiet = TRUE;
            break;
        case ':':
            return elog(1, "option `-%c' requires an argument", optopt);
        default:
            return elog(1, "invalid option `-%c'", optopt);
        }
    }

    if (showhelp)
        return help_usage("board-show");

    i = optind;
    do {
        args.board = argv[i];
        if ((status =
             tman_board_get(tmancfg.base.task, &args, ctx)) != LIBTMAN_OK) {
            if (quiet == FALSE)
                elog(status, errfmt, argv[i], tman_strerror(status));
            continue;
        }

        printf("%-7s : %s\n", "board", args.board);
        for (units = ctx->units; units; units = units->next)
            printf("%-7s : %s\n", units->key, units->val);

        // TODO: add plugin output
    } while (++i < argc);

    return status;
}

static int _board_sync(int argc, char **argv, tman_ctx_t * ctx)
{
    tman_arg_t args;
    int c, i, quiet, showhelp, status;
    const char *errfmt = "cannot switch to '%s': %s";
    int switch_toggle, switch_dir;

    quiet = showhelp = FALSE;
    switch_toggle = switch_dir = TRUE;
    args.project = args.board = args.taskid = NULL;
    while ((c = getopt(argc, argv, ":hnp:qN")) != -1) {
        switch (c) {
        case 'h':
            showhelp = TRUE;
            break;
        case 'n':
            switch_toggle = FALSE;
            break;
        case 'p':
            args.project = optarg;
            break;
        case 'q':
            quiet = TRUE;
            break;
        case 'N':
            switch_dir = FALSE;
            switch_toggle = FALSE;
            break;
        case ':':
            return elog(1, "option `-%c' requires an argument", optopt);
        default:
            return elog(1, "invalid option `-%c'", optopt);
        }
    }

    if (showhelp)
        return help_usage("board-sync");

    if ((status = check_arg_project(&args, errfmt, quiet)))
        return status;

    i = optind;
    do {
        args.board = argv[i];
        if ((status = check_arg_board(&args, errfmt, quiet)))
            return status;
    } while (++i < argc);

    if (status == LIBTMAN_OK && switch_toggle == TRUE) {
        if (toggle_board_set_curr(tmancfg.base.task, &args) && quiet == FALSE)
            status = elog(1, "could not update board toggles");
    }

    return status == LIBTMAN_OK && switch_dir ? tman_pwd_board(&args) : status;
}

static const builtin_t board_commands[] = {
    {.name = "add",.func = &_board_add},
    {.name = "del",.func = &_board_del},
    {.name = "list",.func = &_board_list},
    {.name = "prev",.func = &_board_prev},
    {.name = "move",.func = &_board_move},
    {.name = "set",.func = &_board_set},
    {.name = "show",.func = &_board_show},
    {.name = "sync",.func = &_board_sync},
};

int tman_cli_board(int argc, char **argv, tman_ctx_t * ctx)
{
    char *cmd = argv[1] != NULL ? argv[1] : "list";

    for (int i = 0; i < ARRAY_SIZE(board_commands); ++i)
        if (strcmp(cmd, board_commands[i].name) == 0)
            return board_commands[i].func(argc - 1, argv + 1, ctx);

    return elog(1, "'%s': no such board subcommand", cmd);
}
